syntax = "proto3";

package robot;

message RobotState {
    repeated double joint_angles = 1;
    repeated double joint_velocities = 2;
    repeated double tcp_pose = 3;
    repeated double tcp_orientation = 4;
    repeated double gripper_position = 5;
    repeated double gripper_force = 6;
}